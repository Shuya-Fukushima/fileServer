package com.shiku.file.util.inflastructure.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

import com.shiku.file.util.inflastructure.resource.ExtensionMaster;

@Repository
public interface ExtensionMasterRepository extends JpaRepository<ExtensionMaster, Integer>, JpaSpecificationExecutor<ExtensionMaster> {
}
